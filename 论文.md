markdown
# 第2章 相关理论与技术基础

## 2.1 推荐系统基础

### 2.1.1 推荐任务定义与应用场景

推荐系统是一类信息过滤系统，旨在根据用户的历史行为、偏好信息或上下文环境，主动为用户推荐其可能感兴趣的物品。形式化地，设用户集合为 \(U = \{u_1, u_2, \dots, u_M\}\)，物品集合为 \(I = \{i_1, i_2, \dots, i_N\}\)。用户 \(u\) 与物品 \(i\) 之间的交互可用一个评分或反馈值 \(r_{ui}\) 表示，所有交互构成用户‑物品交互矩阵 \(\mathbf{R} \in \mathbb{R}^{M \times N}\)。推荐任务的目标是：对于每个用户 \(u\)，预测其对未交互物品 \(i\) 的偏好程度 \(\hat{y}_{ui}\)，并据此生成推荐列表。

推荐系统已广泛应用于各类互联网平台，典型应用场景包括：
- **电子商务**：如亚马逊、淘宝，根据用户购买、浏览历史推荐商品。
- **视频/音乐流媒体**：如YouTube、Netflix、Spotify，推荐用户可能喜欢的视频或歌曲。
- **社交网络**：如微博、Twitter，推荐可能感兴趣的用户或内容。
- **新闻推送**：如今日头条、Google News，个性化推荐新闻文章。
- **在线广告**：根据用户画像推荐广告，提高点击率与转化率。

不同场景对推荐的实时性、多样性、解释性等要求各异，但核心任务均为精准建模用户偏好。

### 2.1.2 显式反馈与隐式反馈

根据用户提供的反馈形式，推荐系统中的交互数据可分为两类：

**显式反馈（Explicit Feedback）**：用户直接表达对物品的喜好程度，例如电影评分（1~5星）、点赞、差评等。显式反馈具有明确的数值含义，能够直观反映用户偏好强度。然而，显式反馈数据往往比较稀疏，用户主动评分的行为较少，获取成本高。

**隐式反馈（Implicit Feedback）**：通过用户的行为间接反映其偏好，例如点击、购买、收藏、播放等。隐式反馈数据量大、易于收集，但其存在固有歧义：用户未交互一个物品，可能是因为不喜欢，也可能是因为从未见过该物品（即“未观测”不等于“负样本”）。尽管如此，隐式反馈已成为工业推荐系统的主流数据形式，本课题所采用的Gowalla、Yelp2018和Amazon‑Book数据集均为隐式反馈数据，交互矩阵中的条目为1表示存在交互，0表示未观测。

### 2.1.3 Top‑K 推荐任务与评价目标

Top‑K推荐是指为每个用户生成包含K个物品的推荐列表，并期望该列表能够尽可能覆盖用户真正感兴趣的物品。常用的评价指标包括Recall@K和NDCG@K：

- **召回率（Recall@K）**：衡量推荐列表中命中的用户真实交互物品占其所有交互物品的比例。
\[
\text{Recall}@K = \frac{1}{|U_{\text{test}}|} \sum_{u \in U_{\text{test}}} \frac{|\mathcal{R}_u(K) \cap \mathcal{T}_u|}{|\mathcal{T}_u|}
\]
其中 \(\mathcal{R}_u(K)\) 是模型推荐的Top‑K物品集合，\(\mathcal{T}_u\) 是用户在测试集中实际交互的物品集合。

- **归一化折损累积增益（NDCG@K）**：同时考虑推荐列表中相关物品的排名位置，对排名靠前的正确物品赋予更高权重。
\[
\text{NDCG}@K = \frac{1}{|U_{\text{test}}|} \sum_{u \in U_{\text{test}}} \frac{\text{DCG}_u(K)}{\text{IDCG}_u(K)}
\]
其中 \(\text{DCG}_u(K) = \sum_{k=1}^K \frac{\text{rel}_{uk}}{\log_2(k+1)}\)，\(\text{rel}_{uk}\) 指示第k个推荐物品是否相关，\(\text{IDCG}_u(K)\) 为理想情况下的最大DCG。

本课题采用全量排序评估协议：对每个测试用户，模型对所有未交互物品进行评分，取分数最高的K个作为推荐列表，然后计算Recall和NDCG，最后对所有用户取平均。K值通常取20。

## 2.2 协同过滤与矩阵分解方法

协同过滤是推荐系统中最经典的方法之一，其核心思想是“物以类聚，人以群分”。本小节介绍基于邻域的协同过滤、矩阵分解模型以及用于排序学习的BPR损失函数。

### 2.2.1 基于邻域的协同过滤

基于邻域的协同过滤方法直接利用用户‑物品交互矩阵计算相似度，分为**基于用户的协同过滤（User‑based CF）**和**基于物品的协同过滤（Item‑based CF）**。

- **User‑based CF**：为目标用户找到与其历史行为最相似的其他用户（邻域），然后将这些用户喜欢的物品推荐给目标用户。预测分数通常表示为邻居用户评分的加权平均：
\[
\hat{y}_{ui} = \frac{\sum_{v \in \mathcal{N}_u} \text{sim}(u,v) \, r_{vi}}{\sum_{v \in \mathcal{N}_u} \text{sim}(u,v)}
\]
其中 \(\mathcal{N}_u\) 是用户 \(u\) 的最近邻集合，\(\text{sim}(u,v)\) 是用户 \(u\) 与 \(v\) 的相似度（如余弦相似度、皮尔逊相关系数），\(r_{vi}\) 是用户 \(v\) 对物品 \(i\) 的评分。

- **Item‑based CF**：根据用户已经交互过的物品，推荐与其相似的物品。物品相似度预先计算并存储，在线推荐时仅需聚合用户历史物品的相似物品：
\[
\hat{y}_{ui} = \frac{\sum_{j \in \mathcal{I}_u} \text{sim}(i,j) \, r_{uj}}{\sum_{j \in \mathcal{I}_u} \text{sim}(i,j)}
\]
其中 \(\mathcal{I}_u\) 是用户 \(u\) 交互过的物品集合，\(\text{sim}(i,j)\) 为物品 \(i\) 与 \(j\) 的相似度。

基于邻域的方法简单、可解释性强，但随着用户和物品数量的增长，相似度计算开销大，且对稀疏数据极为敏感。

### 2.2.2 矩阵分解模型

矩阵分解（Matrix Factorization，MF）是协同过滤领域的重要突破，它通过将用户‑物品交互矩阵分解为两个低维矩阵的乘积，学习用户和物品的隐向量表示。

设用户嵌入矩阵为 \(\mathbf{P} \in \mathbb{R}^{M \times d}\)，物品嵌入矩阵为 \(\mathbf{Q} \in \mathbb{R}^{N \times d}\)，其中 \(d\) 为嵌入维度（通常远小于 \(M,N\)）。对于隐式反馈，预测用户 \(u\) 对物品 \(i\) 的偏好常用内积：
\[
\hat{y}_{ui} = \mathbf{p}_u^\top \mathbf{q}_i
\]

传统的矩阵分解通常以最小化平方误差为目标（如用于显式评分），但对于隐式反馈，更常用的是**贝叶斯个性化排序（BPR）**损失，该损失在2.2.3节详述。矩阵分解相比基于邻域的方法具有以下优势：
- 能够泛化到未观测交互，缓解稀疏性问题；
- 参数规模远小于稠密的相似度矩阵，存储和计算效率高；
- 可通过随机梯度下降等优化算法高效训练。

然而，标准矩阵分解仅建模了用户与物品的一阶交互，无法利用图中更高阶的连通性信息，这正是图神经网络推荐模型（如LightGCN）的改进方向。

### 2.2.3 BPR 排序学习思想

贝叶斯个性化排序（Bayesian Personalized Ranking，BPR）是一种面向隐式反馈的排序优化方法，由Rendle等人于2009年提出。BPR的核心思想是：对于每个用户，已交互的正样本物品的预测分数应当高于未交互的负样本物品。

BPR假设用户对物品的偏好具有**成对排序性质**：对于用户 \(u\)，若物品 \(i\) 为正样本、\(j\) 为负样本，则训练目标使 \(\hat{y}_{ui} > \hat{y}_{uj}\)。基于此，BPR定义了一个基于最大后验估计的损失函数：
\[
\mathcal{L}_{\text{BPR}} = -\sum_{u=1}^{M} \sum_{i \in \mathcal{N}_u} \sum_{j \notin \mathcal{N}_u} \ln \sigma(\hat{y}_{ui} - \hat{y}_{uj}) + \lambda \|\Theta\|^2
\]
其中 \(\mathcal{N}_u\) 是用户 \(u\) 交互过的物品集合，\(\sigma(\cdot)\) 是sigmoid函数将分数差映射到(0,1)区间，\(\Theta\) 表示所有可训练参数（如用户和物品嵌入），\(\lambda\) 是正则化系数。通过最小化BPR损失，模型鼓励正样本分数显著高于负样本。

BPR损失已被广泛应用于矩阵分解、图神经网络等推荐模型中，包括本课题研究的LightGCN。相比逐点回归损失（如MSE），BPR直接优化排序目标，更贴合Top‑K推荐的实际需求。

## 2.3 图推荐模型基础

近年来，图神经网络（Graph Neural Network，GNN）因其对图结构数据的强大建模能力而被广泛应用于推荐系统。用户与物品之间的交互行为天然构成二部图结构，图中不仅包含用户与物品的直接交互，还蕴含通过中间节点连接的高阶协同信息。

### 2.3.1 用户-物品二部图建模

在推荐系统中，用户集合 \(U\) 与物品集合 \(I\) 之间的交互关系可以自然地表示为一个**二部图**（bipartite graph）\(\mathcal{G} = (U, I, \mathcal{E})\)，其中每条边 \((u, i) \in \mathcal{E}\) 表示用户 \(u\) 与物品 \(i\) 之间存在交互行为。由于二部图的特点，同一类节点（用户‑用户、物品‑物品）之间没有直接连边，任何两个用户或两个物品之间的关系都必须通过另一类节点间接传递。

二部图的形式化表示通常采用邻接矩阵。设用户数量为 \(M\)，物品数量为 \(N\)，定义用户‑物品交互矩阵 \(\mathbf{R} \in \{0,1\}^{M \times N}\)，其中 \(R_{ui}=1\) 表示存在交互。则整个二部图的邻接矩阵为：
\[
\mathbf{A} = \begin{pmatrix}
\mathbf{0} & \mathbf{R} \\
\mathbf{R}^\top & \mathbf{0}
\end{pmatrix} \in \mathbb{R}^{(M+N) \times (M+N)}
\]
该矩阵的左上角和右下角为零矩阵，反映同类节点无边连接；右上角和左下角分别为 \(\mathbf{R}\) 和 \(\mathbf{R}^\top\)，记录用户与物品之间的交互。

在二部图上进行消息传播时，节点的度信息至关重要。节点的度 \(d_u = |\mathcal{N}_u|\) 表示用户 \(u\) 交互过的物品数量，物品度 \(d_i = |\mathcal{N}_i|\) 表示物品 \(i\) 被交互过的用户数量。对称归一化的邻接矩阵 \(\tilde{\mathbf{A}} = \mathbf{D}^{-1/2} \mathbf{A} \mathbf{D}^{-1/2}\) 可以避免高度数节点在聚合中主导更新，其中 \(\mathbf{D}\) 为度矩阵。

基于二部图的推荐方法核心思想是：通过多层传播，每个节点的嵌入能够逐步融合多跳邻居的信息，从而捕捉高阶协同信号。

### 2.3.2 图卷积网络基本思想

图卷积网络（Graph Convolutional Network，GCN）由 Kipf 和 Welling 于 2017 年提出，是将卷积操作推广到图数据上的经典模型。GCN 的核心思想可以概括为：**通过聚合邻居节点的特征来更新当前节点的表示**，这一过程可以堆叠多层，使得每个节点逐步获得更远邻居的信息。

GCN 的逐层传播规则为：
\[
\mathbf{H}^{(k+1)} = \sigma\left( \tilde{\mathbf{D}}^{-\frac{1}{2}} \tilde{\mathbf{A}} \tilde{\mathbf{D}}^{-\frac{1}{2}} \mathbf{H}^{(k)} \mathbf{W}^{(k)} \right)
\]
其中：
- \(\tilde{\mathbf{A}} = \mathbf{A} + \mathbf{I}\)：在邻接矩阵上添加自环，使节点在聚合时能够保留自身信息。
- \(\tilde{\mathbf{D}}\) 是 \(\tilde{\mathbf{A}}\) 的度矩阵，\(\tilde{\mathbf{D}}^{-\frac{1}{2}} \tilde{\mathbf{A}} \tilde{\mathbf{D}}^{-\frac{1}{2}}\) 为对称归一化操作。
- \(\mathbf{H}^{(k)}\) 为第 \(k\) 层的节点特征矩阵，\(\mathbf{H}^{(0)} = \mathbf{X}\)（输入特征）。
- \(\mathbf{W}^{(k)}\) 为可学习的权重矩阵，用于特征变换。
- \(\sigma(\cdot)\) 为非线性激活函数（如 ReLU）。

从消息传递的视角理解，GCN 每一层包含三个步骤：消息构建、消息聚合、节点更新。GCN 在节点分类、图分类等任务中表现优异，但对于推荐系统中的协同过滤任务，节点通常只有 ID 特征而没有丰富的语义属性，其中的特征变换和非线性激活可能并不必要，这一观察直接推动了 LightGCN 等简化模型的产生。

### 2.3.3 LightGCN 模型原理

LightGCN 由 He 等人于 2020 年提出，是对 NGCF 的简化和改进。其设计动机基于消融实验发现：在协同过滤任务中，GCN 中的特征变换和非线性激活对模型性能没有正面贡献，反而增加训练难度。因此，LightGCN 去除了这两种操作，仅保留最核心的邻居聚合。

**传播规则（LGC）**：
\[
\mathbf{e}_u^{(k+1)} = \sum_{i \in \mathcal{N}_u} \frac{1}{\sqrt{|\mathcal{N}_u|}\sqrt{|\mathcal{N}_i|}} \mathbf{e}_i^{(k)}, \qquad
\mathbf{e}_i^{(k+1)} = \sum_{u \in \mathcal{N}_i} \frac{1}{\sqrt{|\mathcal{N}_i|}\sqrt{|\mathcal{N}_u|}} \mathbf{e}_u^{(k)}
\]
其中 \(\mathbf{e}_u^{(k)}\)、\(\mathbf{e}_i^{(k)}\) 分别为用户 \(u\) 和物品 \(i\) 在第 \(k\) 层的嵌入，初始层 \(\mathbf{e}_u^{(0)}\)、\(\mathbf{e}_i^{(0)}\) 为可训练的 ID 嵌入。权重为对称归一化系数，且 LGC 中**没有自环**。

**层组合**：为缓解过平滑，将每一层嵌入加权求和作为最终表示：
\[
\mathbf{e}_u = \sum_{k=0}^{K} \alpha_k \mathbf{e}_u^{(k)}, \quad
\mathbf{e}_i = \sum_{k=0}^{K} \alpha_k \mathbf{e}_i^{(k)}
\]
通常取 \(\alpha_k = 1/(K+1)\)（均匀权重）。

**预测与训练**：预测分数 \(\hat{y}_{ui} = \mathbf{e}_u^\top \mathbf{e}_i\)，采用 BPR 损失优化初始嵌入。LightGCN 的可训练参数与标准矩阵分解相同，复杂度极低。

### 2.3.4 NGCF 模型原理

NGCF（Neural Graph Collaborative Filtering）由 Wang 等人于 2019 年提出，是首个将图卷积显式应用于协同过滤的模型。NGCF 直接继承了 GCN 的设计范式，在每一层中包含**特征变换**、**邻居聚合**和**非线性激活**。

NGCF 的传播规则（以用户层为例）：
\[
\mathbf{e}_u^{(k+1)} = \sigma\left( \mathbf{W}_1 \mathbf{e}_u^{(k)} + \sum_{i \in \mathcal{N}_u} \frac{1}{\sqrt{|\mathcal{N}_u||\mathcal{N}_i|}} \left( \mathbf{W}_1 \mathbf{e}_i^{(k)} + \mathbf{W}_2 (\mathbf{e}_i^{(k)} \odot \mathbf{e}_u^{(k)}) \right) \right)
\]
其中 \(\mathbf{W}_1, \mathbf{W}_2\) 为可学习的特征变换矩阵，\(\odot\) 为逐元素乘积，\(\sigma\) 为非线性激活函数。物品层更新规则类似。最终将各层嵌入拼接作为最终表示。

尽管 NGCF 在当时取得了最先进的性能，但其特征变换和非线性激活引入了大量参数，导致训练困难、容易过拟合。后续的 LightGCN 通过消融实验证实移除这些操作后性能反而提升。

## 2.4 实验评价指标与实验规范

为了客观评估推荐模型的性能，必须采用统一的评价指标和严格的实验规范。

### 2.4.1 Precision@K、Recall@K、NDCG@K

- **Precision@K（精确率）**：推荐列表中相关物品的比例。
\[
\text{Precision}@K = \frac{1}{|U_{\text{test}}|} \sum_{u \in U_{\text{test}}} \frac{|\mathcal{R}_u(K) \cap \mathcal{T}_u|}{K}
\]

- **Recall@K（召回率）**：推荐列表中命中的相关物品占用户所有相关物品的比例。
\[
\text{Recall}@K = \frac{1}{|U_{\text{test}}|} \sum_{u \in U_{\text{test}}} \frac{|\mathcal{R}_u(K) \cap \mathcal{T}_u|}{|\mathcal{T}_u|}
\]

- **NDCG@K（归一化折损累积增益）**：同时考虑相关性和排名位置。
\[
\text{NDCG}@K = \frac{1}{|U_{\text{test}}|} \sum_{u \in U_{\text{test}}} \frac{\text{DCG}_u(K)}{\text{IDCG}_u(K)},
\quad \text{DCG}_u(K) = \sum_{k=1}^{K} \frac{\text{rel}_{uk}}{\log_2(k+1)}
\]

本文主要采用 Recall@20 和 NDCG@20，辅以 Precision@20。评估采用全量排序协议。

### 2.4.2 多随机种子与稳定性评估

为消除随机性影响，本文对每个实验设置使用 5 个不同随机种子独立运行，报告 **平均值 ± 标准差**，并使用配对 t 检验（p < 0.05 视为显著）判断改进是否显著。

### 2.4.3 收敛性与训练成本评估

- **收敛性**：记录每轮训练的损失值和验证集指标，绘制曲线，对比收敛速度。
- **训练成本**：记录每轮平均时间、总训练时间、显存/内存占用、可训练参数量。

## 2.5 本章小结

本章系统介绍了推荐系统的基础理论、经典方法以及实验评价规范。首先阐述了推荐的定义、场景、反馈类型及 Top‑K 评价目标。随后详细介绍了基于邻域的协同过滤、矩阵分解及 BPR 排序学习。在第三节中，介绍了图推荐模型基础，包括二部图建模、GCN 基本思想、LightGCN 和 NGCF 原理与区别。最后明确了实验评价指标与规范，为后续实验设计奠定了基础。